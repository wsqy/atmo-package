#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ansi-to-html/bin/ansi-to-html" "$@"
  ret=$?
else 
  node  "$basedir/../ansi-to-html/bin/ansi-to-html" "$@"
  ret=$?
fi
exit $ret

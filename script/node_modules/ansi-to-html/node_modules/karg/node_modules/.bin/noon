#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../noon/bin/noon" "$@"
  ret=$?
else 
  node  "$basedir/../noon/bin/noon" "$@"
  ret=$?
fi
exit $ret
